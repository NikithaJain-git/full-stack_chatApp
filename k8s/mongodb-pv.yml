kind: PersistentVolume
apiVersion: v1

metadata:
  name: mangodb-pv

spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data/mongodb
  persistentVolumeReclaimPolicy: Retain
